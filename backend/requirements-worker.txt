-r requirements-base.txt
# YouTube changes its site frequently enough that old yt-dlp pins routinely
# break with "No video formats found" errors — keep this reasonably current
# rather than pinning to whatever was latest when this was written.
yt-dlp==2026.7.4
# whisperx pulls its own compatible faster-whisper/ctranslate2/torch stack,
# including the nvidia-cublas-cu12/nvidia-cudnn-cu12 versions torch itself
# needs (which do NOT match the versions ctranslate2-alone needed pre-2.0 -
# torch is strict about exact companion package versions). Exact resolved
# versions are pinned below from a real `pip install` against this
# requirements file, not guessed.
whisperx==3.8.6
# faster-whisper's utils module imports requests directly but doesn't declare
# it, relying on a transitive pull from huggingface-hub that newer
# huggingface-hub releases no longer provide. Pin explicitly.
requests==2.32.3
